import React from "react";
import Logout from "../components/Logout";

type Props = {};

export default function HomePage({}: Props) {
    return (
        <div>
            <Logout></Logout>
        </div>
    );
}
